/**
 * Peerio Cache module
 *
 * Cache module provides universal access to data returning cached data when available
 * and handling cache sync with server.
 */

var Peerio = this.Peerio || {};

(function(){
    'use strict';

    //-- PUBLIC API ----------------------------------------------------------------------------------------------------
    Peerio.Cache = {
        getSettings: getSettings
    };

    function getSettings(){

    }


}());